<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.editora</groupId>
    <artifactId>editora</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Editora</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>25</maven.compiler.release>
        <!-- maven.build.timestamp is UTC; baked into build-info.properties for the About dialog.
             The indirection via build.timestamp is the standard workaround for the format being
             ignored when maven.build.timestamp is filtered into a resource directly. -->
        <maven.build.timestamp.format>yyyy-MM-dd HH:mm:ss 'UTC'</maven.build.timestamp.format>
        <build.timestamp>${maven.build.timestamp}</build.timestamp>
        <javafx.version>25</javafx.version>
        <javafx.plugin.version>0.0.8</javafx.plugin.version>
        <!-- Personal fork of RichTextFX (adds multiple cursors + column/box selection).
             groupId io.github.adriandeleon, vendored in the in-project m2-repo (see <repositories>).
             Source: https://github.com/adriandeleon/RichTextFX (feat/multi-caret-box-selection). -->
        <richtextfx.version>0.11.7-multicaret.3</richtextfx.version>
        <atlantafx.version>2.1.0</atlantafx.version>
        <jackson.version>2.19.0</jackson.version>
        <junit.version>5.11.4</junit.version>
        <tm4e.version>RELEASE260</tm4e.version>
        <joni.version>2.2.6</joni.version>
        <jcodings.version>1.0.58</jcodings.version>
        <gson.version>2.11.0</gson.version>
        <lsp4j.version>0.23.1</lsp4j.version>
        <commonmark.version>0.28.0</commonmark.version>
        <autolink.version>0.12.0</autolink.version>
        <!-- 1.7.2, not 2.0.0: 2.0.0's module-info exports an empty package (com.github.weisj.jsvg.provider)
             which jlink rejects in the native `dist` build ("exported package not present"). -->
        <jsvg.version>1.7.2</jsvg.version>
        <pdfbox.version>3.0.5</pdfbox.version>
        <lucene.version>10.2.2</lucene.version>
        <javadiffutils.version>4.12</javadiffutils.version>
        <sshd.version>2.15.0</sshd.version>
        <slf4j.version>2.0.17</slf4j.version>
        <moditect.version>1.2.2.Final</moditect.version>
        <jpackage.plugin.version>1.6.6</jpackage.plugin.version>
        <jpackage.type>APP_IMAGE</jpackage.type>
        <!-- App icon for the native installer; overridden per OS (.icns/.ico/.png). -->
        <jpackage.icon>${project.basedir}/branding/editora.png</jpackage.icon>
        <!-- Transitive automatic-module deps patched by moditect for jlink/jpackage. -->
        <reactfx.version>2.0-M5</reactfx.version>
        <undofx.version>2.1.1</undofx.version>
        <flowless.version>0.7.4</flowless.version>
        <wellbehavedfx.version>0.3.3</wellbehavedfx.version>
    </properties>

    <!-- In-project Maven repository holding the personal RichTextFX fork
         (io.github.adriandeleon:richtextfx). Checked into the repo so CI/CD resolves it with no
         external registry or credentials. To update: rebuild the fork, then copy the new
         richtextfx-<version>.{jar,pom} under m2-repo/ and bump <richtextfx.version> above. -->
    <repositories>
        <repository>
            <id>in-project-fork</id>
            <name>In-project RichTextFX fork</name>
            <url>file://${project.basedir}/m2-repo</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>io.github.adriandeleon</groupId>
            <artifactId>richtextfx</artifactId>
            <version>${richtextfx.version}</version>
            <!-- The fork's pom pins JavaFX 21.0.5 (with a mac-aarch64 classifier baked in). Letting that
                 transitively onto the path mixes javafx.base/graphics 21.0.5 with our javafx.controls 25 —
                 e.g. Spinner crashes with NoSuchFieldError AccessibleAttribute.VALUE_STRING. Exclude it so
                 only our JavaFX 25 (declared above) is used. -->
            <exclusions>
                <exclusion>
                    <groupId>org.openjfx</groupId>
                    <artifactId>*</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.github.mkpaz</groupId>
            <artifactId>atlantafx-base</artifactId>
            <version>${atlantafx.version}</version>
        </dependency>
        <!-- Markdown preview: commonmark-java + GFM extensions (all proper JPMS modules). -->
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark</artifactId>
            <version>${commonmark.version}</version>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark-ext-gfm-tables</artifactId>
            <version>${commonmark.version}</version>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark-ext-gfm-strikethrough</artifactId>
            <version>${commonmark.version}</version>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark-ext-task-list-items</artifactId>
            <version>${commonmark.version}</version>
        </dependency>
        <dependency>
            <groupId>org.commonmark</groupId>
            <artifactId>commonmark-ext-autolink</artifactId>
            <version>${commonmark.version}</version>
        </dependency>
        <dependency>
            <groupId>org.nibor.autolink</groupId>
            <artifactId>autolink</artifactId>
            <version>${autolink.version}</version>
        </dependency>
        <!-- SVG rasterizer for the Markdown preview (e.g. shields.io / GitHub badge images, which are
             served as SVG and which JavaFX's image decoder can't read). Lightweight, pure-Java, and a
             proper JPMS module (open, requires transitive java.desktop) — no moditect patching needed. -->
        <dependency>
            <groupId>com.github.weisj</groupId>
            <artifactId>jsvg</artifactId>
            <version>${jsvg.version}</version>
        </dependency>
        <!-- Apache PDFBox: PDF export (code/Markdown). Pulls pdfbox-io + fontbox + commons-logging
             (all automatic modules — patched by moditect for the jlink dist build). The optional
             BouncyCastle (encryption) and imageio codecs are not pulled. -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.dataformat</groupId>
            <artifactId>jackson-dataformat-toml</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- Spell checking: Apache Lucene's pure-Java Hunspell engine (analysis-common, pulls
             lucene-core). Lucene 10 ships proper JPMS modules, so no moditect patching is needed. -->
        <dependency>
            <groupId>org.apache.lucene</groupId>
            <artifactId>lucene-analysis-common</artifactId>
            <version>${lucene.version}</version>
        </dependency>

        <!-- TextMate grammar engine (Eclipse tm4e core, repackaged as a plain Maven jar by the
             NetBeans project) plus its Oniguruma regex backend (joni/jcodings) and JSON parser
             (gson). All are automatic modules patched by moditect for the jlink/jpackage dist. -->
        <dependency>
            <groupId>org.netbeans.external</groupId>
            <artifactId>org.eclipse.tm4e.core-0.14.0</artifactId>
            <version>${tm4e.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jruby.joni</groupId>
            <artifactId>joni</artifactId>
            <version>${joni.version}</version>
        </dependency>
        <dependency>
            <groupId>org.jruby.jcodings</groupId>
            <artifactId>jcodings</artifactId>
            <version>${jcodings.version}</version>
        </dependency>
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <!-- Language Server Protocol client (Eclipse LSP4J) + its JSON-RPC transport. Both are automatic
             modules (filename-derived: org.eclipse.lsp4j / org.eclipse.lsp4j.jsonrpc) patched by moditect
             for the jlink/jpackage dist build; jsonrpc uses gson (already a dependency above). -->
        <dependency>
            <groupId>org.eclipse.lsp4j</groupId>
            <artifactId>org.eclipse.lsp4j</artifactId>
            <version>${lsp4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.lsp4j</groupId>
            <artifactId>org.eclipse.lsp4j.jsonrpc</artifactId>
            <version>${lsp4j.version}</version>
        </dependency>
        <!-- Debug Adapter Protocol client (DAP types + DSPLauncher), used by com.editora.dap for Java
             debugging via jdtls + the Microsoft java-debug plugin. Automatic module (org.eclipse.lsp4j.debug),
             patched by moditect for the dist build; reuses jsonrpc/gson above. -->
        <dependency>
            <groupId>org.eclipse.lsp4j</groupId>
            <artifactId>org.eclipse.lsp4j.debug</artifactId>
            <version>${lsp4j.version}</version>
        </dependency>

        <!-- Diff viewer: java-diff-utils (Myers line diff + unified-diff/patch generation). An automatic
             module (Automatic-Module-Name: io.github.javadiffutils, deps only java.base) — moditect injects
             a real descriptor for the jlink dist build (like pdfbox). Intra-line word highlighting is done
             in-house on top of it (com.editora.diff.InlineDiff), so no second diff library is needed. -->
        <dependency>
            <groupId>io.github.java-diff-utils</groupId>
            <artifactId>java-diff-utils</artifactId>
            <version>${javadiffutils.version}</version>
        </dependency>

        <!-- Apache MINA SSHD — SFTP client + a real nio FileSystemProvider, so remote (SFTP) files reuse
             the app's existing Files.*/Path machinery. We use the combined sshd-osgi bundle (module
             org.apache.sshd.osgi) instead of sshd-common+sshd-core: those two split the
             org.apache.sshd.common/server packages across two automatic modules (illegal under JPMS).
             sshd-sftp then excludes them so the split can't reappear transitively. Automatic modules →
             moditect injects descriptors for the jlink dist build (like pdfbox/tm4e/lsp4j). -->
        <dependency>
            <groupId>org.apache.sshd</groupId>
            <artifactId>sshd-osgi</artifactId>
            <version>${sshd.version}</version>
            <exclusions>
                <!-- jcl-over-slf4j provides module org.apache.commons.logging, which collides with
                     pdfbox's commons-logging (same module name) and breaks jlink/jpackage. We already
                     have the real commons-logging, so drop the bridge. -->
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>jcl-over-slf4j</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>org.apache.sshd</groupId>
            <artifactId>sshd-sftp</artifactId>
            <version>${sshd.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>org.apache.sshd</groupId>
                    <artifactId>sshd-core</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.apache.sshd</groupId>
                    <artifactId>sshd-common</artifactId>
                </exclusion>
                <!-- The JCL→SLF4J bridge provides module org.apache.commons.logging, colliding with
                     pdfbox's commons-logging under jlink/jpackage; we keep the real commons-logging. -->
                <exclusion>
                    <groupId>org.slf4j</groupId>
                    <artifactId>jcl-over-slf4j</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- MINA SSHD pins slf4j-api 1.7.36 (an automatic module jlink can't link); force 2.0.17, which
             is a real JPMS module (and API-compatible for SSHD's logging calls). -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <!-- Filter only build-info.properties so ${maven.build.timestamp} is substituted;
                 everything else (grammars, CSS, FXML) is copied verbatim. -->
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>com/editora/build-info.properties</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>com/editora/build-info.properties</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
            </plugin>
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>${javafx.plugin.version}</version>
                <configuration>
                    <mainClass>com.editora/com.editora.App</mainClass>
                    <options>
                        <option>--enable-native-access=javafx.graphics</option>
                        <!-- Match the packaged app's Prism texture caps (see the dist profile). -->
                        <option>-Dprism.maxvram=2G</option>
                        <option>-Dprism.maxTextureSize=16384</option>
                        <!-- Match the packaged app's heap/GC so dev and prod behave alike.
                             SerialGC: a mostly-idle single-user editor on a small heap gets sub-ms
                             pauses with no parallel GC-thread overhead (lower idle RSS than G1).
                             -Xmx2g caps GC-driven RSS growth while staying safe for a 50 MB file
                             (the huge-file read cap) with deep undo. See the dist profile. -->
                        <option>-Xmx2g</option>
                        <option>-XX:+UseSerialGC</option>
                    </options>
                    <launcher>editora</launcher>
                    <jlinkImageName>editora</jlinkImageName>
                    <jlinkZipName>editora</jlinkZipName>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.moditect</groupId>
                <artifactId>moditect-maven-plugin</artifactId>
                <version>${moditect.version}</version>
                <executions>
                    <execution>
                        <id>patch-automatic-modules</id>
                        <phase>package</phase>
                        <goals>
                            <goal>add-module-info</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/modules</outputDirectory>
                            <overwriteExistingFiles>true</overwriteExistingFiles>
                            <jdepsExtraArgs>
                                <arg>--multi-release=25</arg>
                                <!-- PDFBox references optional BouncyCastle (encryption) + jai/jbig2
                                     image codecs that we don't bundle; let jdeps skip them. -->
                                <arg>--ignore-missing-deps</arg>
                            </jdepsExtraArgs>
                            <modules>
                                <module>
                                    <artifact>
                                        <groupId>org.reactfx</groupId>
                                        <artifactId>reactfx</artifactId>
                                        <version>${reactfx.version}</version>
                                    </artifact>
                                    <moduleInfoSource>
                                        module reactfx {
                                            requires javafx.base;
                                            requires javafx.graphics;
                                            requires javafx.controls;
                                            exports org.reactfx;
                                            exports org.reactfx.collection;
                                            exports org.reactfx.inhibeans;
                                            exports org.reactfx.inhibeans.binding;
                                            exports org.reactfx.inhibeans.collection;
                                            exports org.reactfx.inhibeans.property;
                                            exports org.reactfx.inhibeans.value;
                                            exports org.reactfx.util;
                                            exports org.reactfx.value;
                                        }
                                    </moduleInfoSource>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>org.fxmisc.wellbehaved</groupId>
                                        <artifactId>wellbehavedfx</artifactId>
                                        <version>${wellbehavedfx.version}</version>
                                    </artifact>
                                    <moduleInfoSource>
                                        module wellbehavedfx {
                                            requires javafx.base;
                                            requires javafx.graphics;
                                            requires javafx.controls;
                                            exports org.fxmisc.wellbehaved.event;
                                            exports org.fxmisc.wellbehaved.event.template;
                                            exports org.fxmisc.wellbehaved.event.internal;
                                        }
                                    </moduleInfoSource>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>org.fxmisc.undo</groupId>
                                        <artifactId>undofx</artifactId>
                                        <version>${undofx.version}</version>
                                    </artifact>
                                    <moduleInfoSource>
                                        module org.fxmisc.undo {
                                            requires javafx.base;
                                            requires reactfx;
                                            exports org.fxmisc.undo;
                                            exports org.fxmisc.undo.impl;
                                        }
                                    </moduleInfoSource>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>org.fxmisc.flowless</groupId>
                                        <artifactId>flowless</artifactId>
                                        <version>${flowless.version}</version>
                                    </artifact>
                                    <moduleInfoSource>
                                        module org.fxmisc.flowless {
                                            requires javafx.base;
                                            requires javafx.graphics;
                                            requires javafx.controls;
                                            requires reactfx;
                                            exports org.fxmisc.flowless;
                                        }
                                    </moduleInfoSource>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>io.github.adriandeleon</groupId>
                                        <artifactId>richtextfx</artifactId>
                                        <version>${richtextfx.version}</version>
                                    </artifact>
                                    <moduleInfoSource>
                                        module org.fxmisc.richtext {
                                            requires javafx.base;
                                            requires javafx.graphics;
                                            requires javafx.controls;
                                            requires reactfx;
                                            requires org.fxmisc.flowless;
                                            requires org.fxmisc.undo;
                                            requires wellbehavedfx;
                                            exports org.fxmisc.richtext;
                                            exports org.fxmisc.richtext.event;
                                            exports org.fxmisc.richtext.model;
                                            exports org.fxmisc.richtext.util;
                                            exports org.fxmisc.richtext.multi;
                                        }
                                    </moduleInfoSource>
                                </module>
                                <!-- tm4e core ships only an Automatic-Module-Name; jlink needs a real
                                     descriptor. Its joni/jcodings/gson deps are already modular. The
                                     optional snakeyaml/batik paths (YAML sources, CSS themes) are never
                                     exercised, so they are intentionally not required. -->
                                <module>
                                    <artifact>
                                        <groupId>org.netbeans.external</groupId>
                                        <artifactId>org.eclipse.tm4e.core-0.14.0</artifactId>
                                        <version>${tm4e.version}</version>
                                    </artifact>
                                    <moduleInfoSource>
                                        module org.eclipse.tm4e.core {
                                            requires java.xml;
                                            requires org.jruby.joni;
                                            requires org.jruby.jcodings;
                                            requires com.google.gson;
                                            exports org.eclipse.tm4e.core;
                                            exports org.eclipse.tm4e.core.grammar;
                                            exports org.eclipse.tm4e.core.registry;
                                            exports org.eclipse.tm4e.core.model;
                                            exports org.eclipse.tm4e.core.theme;
                                        }
                                    </moduleInfoSource>
                                </module>
                                <!-- Apache PDFBox 3.x trio ships only Automatic-Module-Name, so jlink
                                     needs real descriptors (jdeps-generated). Listed in dependency order.
                                     (commons-logging 1.3.5 is already a real multi-release module, so it
                                     is NOT patched here.) -->
                                <module>
                                    <artifact>
                                        <groupId>org.apache.pdfbox</groupId>
                                        <artifactId>pdfbox-io</artifactId>
                                        <version>${pdfbox.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>org.apache.pdfbox.io</name>
                                    </moduleInfo>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>org.apache.pdfbox</groupId>
                                        <artifactId>fontbox</artifactId>
                                        <version>${pdfbox.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>org.apache.fontbox</name>
                                    </moduleInfo>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>org.apache.pdfbox</groupId>
                                        <artifactId>pdfbox</artifactId>
                                        <version>${pdfbox.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>org.apache.pdfbox</name>
                                    </moduleInfo>
                                </module>
                                <!-- LSP4J (+ its JSON-RPC transport) ship only Bundle-SymbolicName, so
                                     jlink needs real descriptors. jsonrpc requires gson (already modular);
                                     lsp4j requires jsonrpc. Listed in dependency order.
                                     `<open>true</open>` makes them open modules so gson can reflect into
                                     lsp4j's DTOs (ClientCapabilities, the jsonrpc.messages types, …) for
                                     JSON-RPC (de)serialization — under jlink a real module otherwise does
                                     not open its packages to com.google.gson and `initialize` throws
                                     InaccessibleObjectException. (On the classpath every automatic module is
                                     already open, so this only bites the dist build.) -->
                                <module>
                                    <artifact>
                                        <groupId>org.eclipse.lsp4j</groupId>
                                        <artifactId>org.eclipse.lsp4j.jsonrpc</artifactId>
                                        <version>${lsp4j.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>org.eclipse.lsp4j.jsonrpc</name>
                                        <open>true</open>
                                    </moduleInfo>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>org.eclipse.lsp4j</groupId>
                                        <artifactId>org.eclipse.lsp4j</artifactId>
                                        <version>${lsp4j.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>org.eclipse.lsp4j</name>
                                        <open>true</open>
                                    </moduleInfo>
                                </module>
                                <!-- DAP JSON-RPC transport: required by lsp4j.debug (must be patched
                                     first, in dependency order); requires jsonrpc. -->
                                <module>
                                    <artifact>
                                        <groupId>org.eclipse.lsp4j</groupId>
                                        <artifactId>org.eclipse.lsp4j.jsonrpc.debug</artifactId>
                                        <version>${lsp4j.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>org.eclipse.lsp4j.jsonrpc.debug</name>
                                        <open>true</open>
                                    </moduleInfo>
                                </module>
                                <!-- DAP client: same treatment as lsp4j; requires jsonrpc + jsonrpc.debug. -->
                                <module>
                                    <artifact>
                                        <groupId>org.eclipse.lsp4j</groupId>
                                        <artifactId>org.eclipse.lsp4j.debug</artifactId>
                                        <version>${lsp4j.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>org.eclipse.lsp4j.debug</name>
                                        <open>true</open>
                                    </moduleInfo>
                                </module>
                                <!-- java-diff-utils (diff viewer): Automatic-Module-Name only, deps just
                                     java.base — jdeps generates a clean descriptor. Not signed, so no
                                     signature-strip is needed in the antrun step below. -->
                                <module>
                                    <artifact>
                                        <groupId>io.github.java-diff-utils</groupId>
                                        <artifactId>java-diff-utils</artifactId>
                                        <version>${javadiffutils.version}</version>
                                    </artifact>
                                    <moduleInfo>
                                        <name>io.github.javadiffutils</name>
                                    </moduleInfo>
                                </module>
                                <!-- Apache MINA SSHD (SFTP remote files): the combined sshd-osgi bundle
                                     first (so sshd-sftp's descriptor can require it by name), then
                                     sshd-sftp. Automatic modules, not signed (no signature-strip needed).
                                     jdeps with the ignore-missing-deps arg handles optional deps such as
                                     bouncycastle. -->
                                <module>
                                    <artifact>
                                        <groupId>org.apache.sshd</groupId>
                                        <artifactId>sshd-osgi</artifactId>
                                        <version>${sshd.version}</version>
                                    </artifact>
                                    <!-- Hand-maintained descriptor: the jdeps-generated one (106 exports +
                                         the RootedFileSystemProvider service) plus `requires org.slf4j`,
                                         which jdeps drops under ignore-missing-deps — without it every SSHD
                                         class fails at load time (its static slf4j Logger is unreadable). -->
                                    <moduleInfoFile>${project.basedir}/src/moditect/module-info-sshd-osgi.java</moduleInfoFile>
                                </module>
                                <module>
                                    <artifact>
                                        <groupId>org.apache.sshd</groupId>
                                        <artifactId>sshd-sftp</artifactId>
                                        <version>${sshd.version}</version>
                                    </artifact>
                                    <!-- Hand-written: client-only. jdeps under ignore-missing-deps emits no
                                         requires and a provides for the SFTP *server* SubsystemFactory we
                                         never use, which jlink then rejects (sftp can't read the service's
                                         package). We export just the client packages the app touches and
                                         keep only the nio FileSystemProvider service. -->
                                    <moduleInfoSource>
                                        module org.apache.sshd.sftp {
                                            requires org.apache.sshd.osgi;
                                            requires org.slf4j;
                                            exports org.apache.sshd.sftp;
                                            exports org.apache.sshd.sftp.client;
                                            exports org.apache.sshd.sftp.client.fs;
                                            exports org.apache.sshd.sftp.common;
                                            provides java.nio.file.spi.FileSystemProvider
                                                with org.apache.sshd.sftp.client.fs.SftpFileSystemProvider;
                                        }
                                    </moduleInfoSource>
                                </module>
                            </modules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- OS-activated profiles select the native installer type for the `dist` build. -->
        <profile>
            <id>os-mac</id>
            <activation><os><family>mac</family></os></activation>
            <properties>
                <jpackage.type>DMG</jpackage.type>
                <jpackage.icon>${project.basedir}/branding/editora.icns</jpackage.icon>
            </properties>
        </profile>
        <profile>
            <id>os-windows</id>
            <activation><os><family>windows</family></os></activation>
            <properties>
                <jpackage.type>MSI</jpackage.type>
                <jpackage.icon>${project.basedir}/branding/editora.ico</jpackage.icon>
            </properties>
        </profile>
        <profile>
            <id>os-linux</id>
            <activation><os><name>Linux</name></os></activation>
            <properties>
                <jpackage.type>DEB</jpackage.type>
                <jpackage.icon>${project.basedir}/branding/editora.png</jpackage.icon>
            </properties>
        </profile>

        <!-- `mvn -Pdist package` builds a self-contained app image / native installer. -->
        <profile>
            <id>dist</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <version>3.8.1</version>
                        <executions>
                            <execution>
                                <id>stage-dependencies</id>
                                <phase>prepare-package</phase>
                                <goals>
                                    <goal>copy-dependencies</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/mods</outputDirectory>
                                    <includeScope>runtime</includeScope>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>3.1.0</version>
                        <executions>
                            <execution>
                                <id>assemble-module-path</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <!-- Overlay moditect-patched modules over the plain automatic jars. -->
                                        <copy todir="${project.build.directory}/mods" overwrite="true">
                                            <fileset dir="${project.build.directory}/modules"/>
                                        </copy>
                                        <!-- Add the application module itself. -->
                                        <copy todir="${project.build.directory}/mods"
                                              file="${project.build.directory}/${project.build.finalName}.jar"/>
                                        <!-- Drop the empty (non-classifier) JavaFX jars; keep the platform ones. -->
                                        <delete>
                                            <fileset dir="${project.build.directory}/mods"
                                                     includes="javafx-*-${javafx.version}.jar"/>
                                        </delete>
                                        <!-- The NetBeans-repackaged tm4e jar is code-signed; jlink rejects
                                             signed modular jars (and moditect already invalidated the
                                             signature by injecting module-info). Strip the signature blocks.
                                             Done with Ant's zip task so no external `zip` binary is needed. -->
                                        <zip destfile="${project.build.directory}/mods/tm4e-unsigned.jar">
                                            <zipfileset src="${project.build.directory}/mods/org.eclipse.tm4e.core-0.14.0-${tm4e.version}.jar"
                                                        excludes="META-INF/*.SF,META-INF/*.RSA,META-INF/*.DSA,META-INF/*.EC"/>
                                        </zip>
                                        <move file="${project.build.directory}/mods/tm4e-unsigned.jar"
                                              tofile="${project.build.directory}/mods/org.eclipse.tm4e.core-0.14.0-${tm4e.version}.jar"/>
                                        <!-- The lsp4j jars are likewise code-signed (and moditect already
                                             invalidated the signature by injecting module-info); jlink rejects
                                             signed modular jars. Strip the signature blocks the same way. -->
                                        <zip destfile="${project.build.directory}/mods/lsp4j-unsigned.jar">
                                            <zipfileset src="${project.build.directory}/mods/org.eclipse.lsp4j-${lsp4j.version}.jar"
                                                        excludes="META-INF/*.SF,META-INF/*.RSA,META-INF/*.DSA,META-INF/*.EC"/>
                                        </zip>
                                        <move file="${project.build.directory}/mods/lsp4j-unsigned.jar"
                                              tofile="${project.build.directory}/mods/org.eclipse.lsp4j-${lsp4j.version}.jar"/>
                                        <zip destfile="${project.build.directory}/mods/lsp4j-jsonrpc-unsigned.jar">
                                            <zipfileset src="${project.build.directory}/mods/org.eclipse.lsp4j.jsonrpc-${lsp4j.version}.jar"
                                                        excludes="META-INF/*.SF,META-INF/*.RSA,META-INF/*.DSA,META-INF/*.EC"/>
                                        </zip>
                                        <move file="${project.build.directory}/mods/lsp4j-jsonrpc-unsigned.jar"
                                              tofile="${project.build.directory}/mods/org.eclipse.lsp4j.jsonrpc-${lsp4j.version}.jar"/>
                                        <zip destfile="${project.build.directory}/mods/lsp4j-jsonrpc-debug-unsigned.jar">
                                            <zipfileset src="${project.build.directory}/mods/org.eclipse.lsp4j.jsonrpc.debug-${lsp4j.version}.jar"
                                                        excludes="META-INF/*.SF,META-INF/*.RSA,META-INF/*.DSA,META-INF/*.EC"/>
                                        </zip>
                                        <move file="${project.build.directory}/mods/lsp4j-jsonrpc-debug-unsigned.jar"
                                              tofile="${project.build.directory}/mods/org.eclipse.lsp4j.jsonrpc.debug-${lsp4j.version}.jar"/>
                                        <zip destfile="${project.build.directory}/mods/lsp4j-debug-unsigned.jar">
                                            <zipfileset src="${project.build.directory}/mods/org.eclipse.lsp4j.debug-${lsp4j.version}.jar"
                                                        excludes="META-INF/*.SF,META-INF/*.RSA,META-INF/*.DSA,META-INF/*.EC"/>
                                        </zip>
                                        <move file="${project.build.directory}/mods/lsp4j-debug-unsigned.jar"
                                              tofile="${project.build.directory}/mods/org.eclipse.lsp4j.debug-${lsp4j.version}.jar"/>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.panteleyev</groupId>
                        <artifactId>jpackage-maven-plugin</artifactId>
                        <version>${jpackage.plugin.version}</version>
                        <executions>
                            <!-- PHASE 1: always build a jlink'd APP_IMAGE (the installer is wrapped
                                 later, after AOT training, by the aot_build helper). -->
                            <execution>
                                <id>jpackage-app-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>jpackage</goal>
                                </goals>
                                <configuration>
                                    <type>APP_IMAGE</type>
                                    <name>Editora</name>
                                    <appVersion>${project.version}</appVersion>
                                    <vendor>Editora</vendor>
                                    <icon>${jpackage.icon}</icon>
                                    <module>com.editora/com.editora.App</module>
                                    <modulePaths>
                                        <modulePath>${project.build.directory}/mods</modulePath>
                                    </modulePaths>
                                    <javaOptions>
                                        <javaOption>--enable-native-access=javafx.graphics</javaOption>
                                        <!-- Raise the Prism texture caps: the default 512 MB VRAM pool
                                             could be exhausted by many open files' GPU textures, after
                                             which JavaFX NPEs in the render thread (black screen). These
                                             are harmless ceilings, not reservations. -->
                                        <javaOption>-Dprism.maxvram=2G</javaOption>
                                        <javaOption>-Dprism.maxTextureSize=16384</javaOption>
                                        <!-- Heap/GC footprint. With no -Xmx, jpackage inherits the
                                             default ergonomic max heap (~1/4 of physical RAM), which on
                                             a high-RAM machine balloons to many GB and lets GC-driven RSS
                                             grow accordingly. A fixed 2g ceiling keeps RSS predictable
                                             across the release matrix while staying safe for a 50 MB file
                                             (the huge-file read cap) with deep undo history.
                                             SerialGC: for a mostly-idle single-user editor on a small
                                             heap, pauses are sub-millisecond and there's no parallel
                                             GC-thread overhead (lower idle RSS than the default G1). -->
                                        <javaOption>-Xmx2g</javaOption>
                                        <javaOption>-XX:+UseSerialGC</javaOption>
                                        <!-- AOT cache (JDK 25 Leyden) — ~28% faster cold start (measured:
                                             first-frame ~1060ms -> ~760ms on macOS). The cache is TRAINED
                                             after this image is built (see the aot_build exec step below):
                                             a full-GUI training run writes $APPDIR/editora.aot, which this
                                             option then maps at every launch. $APPDIR is expanded by the
                                             jpackage launcher to the read-only install dir, so the cache
                                             rides inside the image. GRACEFUL: if training failed (e.g. no
                                             display on a runner) and editora.aot is absent/invalid, the
                                             default AOTMode=auto logs to stderr (invisible in the packaged
                                             app) and starts normally — a missing cache never breaks launch. -->
                                        <javaOption>-XX:AOTCache=$APPDIR/editora.aot</javaOption>

                                        <!-- ===== Opt-in tunings below: DEVICE-TEST per platform on the
                                             release matrix before enabling. They touch correctness (heap
                                             layout / the render backend), so they don't ship by default. =====

                                        macOS Metal render pipeline (Apple deprecated the OpenGL/es2 backend
                                        JavaFX otherwise uses on macOS). Lower overhead on Apple Silicon and
                                        the forward path; this app is overlay-heavy (minimap, whitespace,
                                        ruler, diagnostic/search canvases) so it should benefit. Newer code
                                        path — verify rendering on a real Mac before shipping. macOS only:
                                        <javaOption>-Dprism.order=mtl,es2,sw</javaOption>

                                        Compact object headers (JDK 25, still experimental): shrinks every
                                        object header 12 -> 8 bytes, ~10-20% live-heap reduction. An editor
                                        holds millions of small objects (style spans, tokens, rope nodes),
                                        so this pays off — but test for stability first (and note it changes
                                        the AOT cache's recorded layout, so retrain after enabling):
                                        <javaOption>-XX:+UnlockExperimentalVMOptions</javaOption>
                                        <javaOption>-XX:+UseCompactObjectHeaders</javaOption> -->
                                    </javaOptions>
                                    <!-- jlink image stripping: smaller runtime -> smaller DMG/MSI/DEB,
                                         faster install, less mapped footprint. no-man-pages/no-header-files
                                         drop docs/headers, strip-debug drops line tables, zip-6 recompresses
                                         the modules. NOTE: strip-native-commands is deliberately NOT in this
                                         list — the bin/java it would remove is needed for the AOT training
                                         run; the aot_build helper deletes bin/ itself, after training,
                                         reclaiming the same footprint. -->
                                    <jLinkOptions>
                                        <jLinkOption>--strip-debug</jLinkOption>
                                        <jLinkOption>--no-man-pages</jLinkOption>
                                        <jLinkOption>--no-header-files</jLinkOption>
                                        <jLinkOption>--compress=zip-6</jLinkOption>
                                    </jLinkOptions>
                                    <destination>${project.build.directory}/aot-image</destination>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- After the APP_IMAGE is built (above), run the single-file aot_build helper with
                         the BUILD JDK: it trains a full-GUI AOT cache against the image's own runtime
                         (xvfb-run on a headless Linux runner), injects $APPDIR/editora.aot, strips the
                         runtime bin/, then either copies the image (APP_IMAGE builds) or wraps it into
                         the DMG/MSI/DEB via jpackage's app-image mode (installer builds) into target/dist.
                         Declared after the jpackage plugin so it runs later in the `package` phase. -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.5.0</version>
                        <executions>
                            <execution>
                                <id>aot-train-and-deliver</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                                <configuration>
                                    <executable>${java.home}/bin/java</executable>
                                    <arguments>
                                        <argument>${project.basedir}/scripts/aot_build.java</argument>
                                        <argument>${project.build.directory}/aot-image</argument>
                                        <argument>Editora</argument>
                                        <argument>${project.version}</argument>
                                        <argument>${jpackage.type}</argument>
                                        <argument>${jpackage.icon}</argument>
                                        <argument>${project.build.directory}/dist</argument>
                                        <argument>com.editora/com.editora.App</argument>
                                    </arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- `mvn -Pfatjar package` builds a single runnable uber-jar for the BUILD HOST'S platform:
             target/Editora-<version>.jar, launchable with `java -jar` on that OS/arch. It runs
             everything from the classpath (unnamed module) via the non-Application
             com.editora.Launcher main class, so the JPMS module path / jlink dist build is
             unaffected. JavaFX's host-platform classifier jar (resolved transitively) carries the
             classes + native libraries; it is bundled as-is.

             A single jar cannot be portable across all platforms: JavaFX's macOS/Linux x64 and arm64
             native libraries share identical filenames, so they collide in one jar. The release CI
             matrix instead runs this profile on each runner to produce one jar per platform
             (see .github/workflows/release.yml). -->
        <profile>
            <id>fatjar</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>3.6.0</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                                <configuration>
                                    <finalName>Editora-${project.version}</finalName>
                                    <createDependencyReducedPom>false</createDependencyReducedPom>
                                    <transformers>
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <mainClass>com.editora.Launcher</mainClass>
                                            <manifestEntries>
                                                <!-- Silence the JavaFX native-access warning on JDK 24+
                                                     (ignored by older JDKs). -->
                                                <Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
                                            </manifestEntries>
                                        </transformer>
                                        <!-- Merge META-INF/services (e.g. Jackson, tm4e SPI files). -->
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                    </transformers>
                                    <filters>
                                        <filter>
                                            <artifact>*:*</artifact>
                                            <excludes>
                                                <!-- The tm4e jar is code-signed; its signature blocks
                                                     would fail jar verification once repackaged. -->
                                                <exclude>META-INF/*.SF</exclude>
                                                <exclude>META-INF/*.RSA</exclude>
                                                <exclude>META-INF/*.DSA</exclude>
                                                <exclude>META-INF/*.EC</exclude>
                                                <!-- Drop module descriptors: the fat jar runs from the
                                                     classpath (unnamed module), and multiple
                                                     module-info.class entries would otherwise collide. -->
                                                <exclude>module-info.class</exclude>
                                                <exclude>META-INF/versions/*/module-info.class</exclude>
                                            </excludes>
                                        </filter>
                                    </filters>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
