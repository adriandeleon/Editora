public class Comments {
    // In a code buffer only comments and strings are spell-checked: occured, untill.
    String msg = "an intentionnal typo in a string literal";
    int total = 0; // identifiers like total are not checked
}
