/* Place the caret after the `{` and press Enter: the new line should indent one
   level; typing the closing `}` should re-align to the opener. */
int main(void) {
    if (1) {
    }
    return 0;
}
