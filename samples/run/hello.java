// Java 25 compact source file (JEP 512): a top-level `void main` with no class.
// The Run feature shows a green ▶ gutter glyph on the `void main(` line (needs JDK 25 on PATH).
void main() {
    IO.println("Hello from an Editora compact-source Java file!");
    for (int i = 1; i <= 3; i++) {
        IO.println("line " + i);
    }
}
